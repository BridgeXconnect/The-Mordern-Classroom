// TODO Phase 2: Generate PPTX from slide JSON via pptxgenjs, stream to client
// POST { lessonId }
export async function POST() {
  return Response.json({ ok: true, message: "TODO: Phase 2 — PPTX export" });
}
