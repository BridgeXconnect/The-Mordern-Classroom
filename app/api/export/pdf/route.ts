// TODO Phase 3: Render worksheet to PDF via @react-pdf/renderer, stream to client
// POST { worksheetId }
export async function POST() {
  return Response.json({ ok: true, message: "TODO: Phase 3 — PDF export" });
}
