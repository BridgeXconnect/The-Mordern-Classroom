// TODO Phase 3: Generate worksheet sections via LLM
// POST { lessonId, lessonTitle, objectives, cefrLevel, sectionTypes, additionalNotes }
export async function POST() {
  return Response.json({ ok: true, message: "TODO: Phase 3 — worksheet generation" });
}
