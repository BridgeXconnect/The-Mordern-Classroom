// TODO Phase 4: Generate image via Pollinations.ai or HF FLUX.1-schnell
// POST { prompt, lessonId, source: "pollinations" | "huggingface" }
export async function POST() {
  return Response.json({ ok: true, message: "TODO: Phase 4 — image generation" });
}
