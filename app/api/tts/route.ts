// TODO Phase 7: Generate TTS audio via Google Cloud TTS, store in R2, return URL
// POST { text, cefrLevel, voiceName }
export async function POST() {
  return Response.json({ ok: true, message: "TODO: Phase 7 — TTS generation" });
}
